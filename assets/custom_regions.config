/*
========================================================================================
    Example custom configuration for region-specific exclusion ranges
========================================================================================
    Copy this file and modify the region_exclusion_ranges map to suit your needs.
    Usage: nextflow run AmstlerStephan/haplotyping-KIV2-nf -c custom_regions.config
----------------------------------------------------------------------------------------
*/

params {
    // Region-specific exclusion ranges
    region_exclusion_ranges = [
        "lpa2645": "",              // No positions to exclude
        "lpa5104": "2472,2506"      // Exclude positions 2472-2506
    ]
}

/*
Notes:
- If a region is not defined in the map, no exclusion ranges will be applied
- Ranges should be provided as comma-separated values: "start1,end1,start2,end2,..."
- Region names must match exactly with your input directory structure
- You can add as many regions as needed
*/